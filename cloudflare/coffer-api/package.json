{
  "name": "aocch-coffer-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply coffer-observations --local",
    "db:migrate:remote": "wrangler d1 migrations apply coffer-observations --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
